# === Server-only secrets (never commit real values) ===
# Google Generative Language API (Gemini)
GEMINI_API_KEY=

# Airtable REST credentials
AIRTABLE_API_KEY=
AIRTABLE_BASE_ID=
# Optional: override default table name
AIRTABLE_TABLE_RESUMES=Resumes

# Deployment context (set automatically on Vercel)
# VERCEL_ENV=development|preview|production
